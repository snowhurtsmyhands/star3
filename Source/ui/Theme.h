#pragma once

#include "CommonIncludes.h"

namespace starflux::ui::theme
{
static const juce::Colour bg = juce::Colour::fromRGB(10, 12, 18);
static const juce::Colour panel = juce::Colour::fromRGB(20, 24, 36);
static const juce::Colour accent = juce::Colour::fromRGB(107, 141, 255);
static const juce::Colour text = juce::Colour::fromRGB(220, 226, 240);
}
